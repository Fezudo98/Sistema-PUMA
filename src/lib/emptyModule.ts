// Stub vazio usado só como alvo do resolveAlias do Turbopack em dev (ver
// next.config.ts) — substitui o módulo nativo "canvas" que o pdfjs-dist importa
// apenas no ramo de execução em Node puro (nunca usado no browser/client component).
export {};
